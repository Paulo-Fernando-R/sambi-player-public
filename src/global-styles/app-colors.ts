const bgDark = "#000000";

const textMediumLight = "#A8A8A8";

const textLight = "#FFFFFF";

const primary = "#2A9235";
const primaryDark = "#013306";

const accent = "#76005C";

const bgBottonNav = "rgba(49, 49, 49, 0.93)";

const bgPopupPlayer = "rgba(0, 0, 0, 0.9)";

const bgInput = "rgba(130, 130, 130, 0.2)"

const bgAnimation = "rgba(0, 0, 0, 0.5)"

const bgGradient = [
    "#000000",
    "#041801",
    "#170016"
]

const appColors = {
    bgDark,
    textMediumLight,
    textLight,
    primaryDark,
    primary,
    accent,
    bgBottonNav,
    bgInput,
    bgGradient,
    bgPopupPlayer,
    bgAnimation
};

export default appColors;
