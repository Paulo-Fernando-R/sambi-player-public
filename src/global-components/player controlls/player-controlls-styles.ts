import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    controls: {
        flexDirection: "row",
        gap: 12,
    },
    controlsLarge: {
        flexDirection: "row",
        gap: 16,
    },
    controlButton: {
        borderRadius: 50,
    },
});

export default styles;
